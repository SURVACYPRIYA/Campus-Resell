{
  "dependencies": {
    "dotenv": "^17.4.2"
  },
  "scripts": {
    "start": "npm --prefix Backend start",
    "dev": "npm --prefix Backend run dev",
    "install": "npm install && npm --prefix Backend install"
  }
}
