# TODO

- [ ] Replace About / FAQ / Contact links in `Frontend/src/components/Navbar.jsx` with a single `Support` dropdown.
- [ ] Ensure dropdown items route to `/story`, `/faq`, `/contact`.
- [ ] Verify dropdown behavior (open/close) works alongside existing profile menu.
- [ ] (After changes) Run frontend to ensure there are no runtime/build errors.

